export const makeProcessorLanguagesList = (keyword: string) => [
  keyword,
  // ASCII
  `${keyword}-annatar`,
  `${keyword}-eldamar`,
  `${keyword}-primate`,
  // CSUR
  `${keyword}-alcarin`,
  `${keyword}-artano`,
  `${keyword}-telcontar`,
  `${keyword}-freemono`,
];
